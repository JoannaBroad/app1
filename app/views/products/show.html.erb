<div class="show-page">
<p id="notice"><%= notice %></p>

<%= image_tag(@product.image_url, class: "col-md-7 col-sm-7 col-xs-12") %>

<div class="col-md-5 col-sm-5 col-xs-12">
<p>
  <strong>Name:</strong>
  <%= @product.name %>
</br><br><br>
  <strong>Description:</strong>
  <%= @product.description %>
<br><br>
  <strong>price:</strong>
  <%= @product.price %>
<br><br>
  <strong>Colour:</strong>
  <%= @product.color %>
<br><br>
  <strong>rating:</strong>
  <%= @product.highest_rating_comment.try(:rating)%>
<br><br>
  <strong>comment:</strong>
"<%= @product.highest_rating_comment.try(:body)%>"</p>
</div>
</div>

<% if signed_in? %>
 <%= render partial: "new_comment" %>
<% end %>

<div class="links" style="clear:both;">
  <%= link_to 'Edit', edit_product_path(@product) %>
  <%= link_to 'Back', products_path %>
</div>
